{
  "name": "app",
  "scripts": {
    "dev": "concurrently --names 'SERVER,CLIENT' -c 'bgRed.bold,bgMagenta.bold' 'npm run dev --workspace=server' 'npm run dev --workspace=client'"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}
